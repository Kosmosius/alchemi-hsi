# Synthetic sensor projection configuration stub.
# Fill in dataset-specific wavelength grids before use.
augmentations:
  random_sensor_project:
    _target_: alchemi.align.RandomSensorProject
    config:
      _target_: alchemi.srf.synthetic.SyntheticSensorConfig
      highres_axis_nm: ${lab_grid_nm}
      n_bands: 64
      center_jitter_nm: 5.0
      fwhm_range_nm: [8.0, 20.0]
      shape: gaussian
      seed: 1337
