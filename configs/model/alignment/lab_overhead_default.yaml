# Alignment losses for lab-to-overhead pairing (Stage B).

enable_contrastive: true
enable_cycle: true
contrastive_weight: 1.0
cycle_weight: 0.5
temperature: 0.07
projection_dim: 256
projection_depth: 2
